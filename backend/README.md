# Pharmacy Starter Backend

Simple Node.js + Express starter backend using sqlite3. It auto-creates `retail.db`.

Endpoints:
- GET /products
- POST /products
- GET /inventory
- POST /purchase
- POST /sales

To run:
1. cd backend
2. npm install
3. node server.js
4. Open http://localhost:3000/