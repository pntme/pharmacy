import { Typography, Box } from '@mui/material';

export default function Sales() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Sales History
      </Typography>
      <Typography variant="body1">
        Sales history and analytics coming soon.
      </Typography>
    </Box>
  );
}
